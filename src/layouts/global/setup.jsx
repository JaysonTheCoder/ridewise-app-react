import React from 'react'
import Login from '../Login'
import HomeScreen from '../HomeScreen'

function Setup({ validated }) {

  return (
    <React.Fragment> 
        <Login/>
    </React.Fragment>
  )
}

export default Setup
